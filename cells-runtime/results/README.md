# Cells Runtime Results

Use `extension-capability-matrix.md` to record manual outcomes from
the Cells runtime extension lab.

The matrix separates native runtime behavior from behavior that could be polyfilled
in the app shell. Treat request blocking, extension background/service worker
lifecycle, content script isolation, and permission handling as native-browser
requirements unless the lab proves otherwise.
